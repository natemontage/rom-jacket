mkdir SOURCE
ATTRIB +H SOURCE
set NWDIR=Sega Bass Fishing
for %%a in ("bass.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Daytona USA 2 Power Edition
for %%a in ("dayto2pe.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Daytona USA 2 (Revision A)
for %%a in ("daytona2.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Dirt Devils (Revision A)
for %%a in ("dirtdvls.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Dirt Devils (alt) (Revision A)
for %%a in ("dirtdvlsa.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Emergency Call Ambulance
for %%a in ("eca.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Emergency Call Ambulance (Export)
for %%a in ("ecax.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Fighting Vipers 2 (Revision A)
for %%a in ("fvipers2.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Get Bass
for %%a in ("getbass.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Harley-Davidson and L.A. Riders (Revision A)
for %%a in ("harley.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Harley-Davidson and L.A. Riders (Revision B)
for %%a in ("harleyb.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=L.A. Machineguns
for %%a in ("lamachin.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=LeMans 24
for %%a in ("lemans24.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=The Lost World
for %%a in ("lostwsga.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Magical Truck Adventure
for %%a in ("magtruck.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=The Ocean Hunter
for %%a in ("oceanhun.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Scud Race (Australia)
for %%a in ("scud.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Scud Race (Export)
for %%a in ("scuda.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Scud Race (Japan)
for %%a in ("scudj.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Scud Race Plus (Revision A)
for %%a in ("scudp.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Ski Champ
for %%a in ("skichamp.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Spikeout Final Edition
for %%a in ("spikeofe.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Spikeout (Revision C)
for %%a in ("spikeout.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Sega Rally 2
for %%a in ("srally2.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Sega Rally 2 DX
for %%a in ("srally2x.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Star Wars Trilogy (Revision A)
for %%a in ("swtrilgy.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Star Wars Trilogy
for %%a in ("swtrilgya.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Virtua Fighter 3 (Revision C)
for %%a in ("vf3.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Virtua Fighter 3 (Revision A)
for %%a in ("vf3a.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Virtua Fighter 3 Team Battle
for %%a in ("vf3tb.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Virtual On 2 - Oratorio Tangram (Revision B)
for %%a in ("von2.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Virtual On 2 - Oratorio Tangram (ver 5.4g)
for %%a in ("von254g.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Virtua Striker 2 (Step 2.0)
for %%a in ("vs2.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Virtua Striker 2 (Step 1.5)
for %%a in ("vs215.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Virtua Striker 2 '98 (Step 2.0)
for %%a in ("vs298.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Virtua Striker 2 '98 (Step 1.5)
for %%a in ("vs29815.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Virtua Striker 2 '99
for %%a in ("vs299.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Virtua Striker 2 '99 (Revision A)
for %%a in ("vs299a.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Virtua Striker 2 '99 (Revision B)
for %%a in ("vs299b.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
set NWDIR=Virtua Striker 2 '99.1 (Revision B)
for %%a in ("vs2v991.zip") do set ROM=%%~a
if exist "%ROM%" mkdir "%CD%\%NWDIR%" &&  move /Y "%ROM%" "%CD%\SOURCE" && mklink "%CD%\%NWDIR%\%ROM%" "%CD%\SOURCE\%ROM%"
